"use client";

import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { ArrowRight, BarChart3, Goal, History, Shield, Star, Trophy, Swords } from "lucide-react";
import FavoriteButton from "../../../components/FavoriteButton";

function normalizeStats(item) { const player = item?.player || {}; const stats = Array.isArray(item?.statistics) ? item.statistics : []; return { player, stats }; }
function sum(stats, group, field) { return stats.reduce((total, entry) => total + Number(entry?.[group]?.[field] || 0), 0); }
function formatDate(value) { if (!value) return "—"; try { return new Intl.DateTimeFormat("fa-IR", { dateStyle: "medium" }).format(new Date(value)); } catch { return value; } }

export default function PlayerDetailPage({ params }) {
  const [data, setData] = useState(null); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  useEffect(() => { let active = true; async function load() { try { const response = await fetch(`/api/football/players?id=${encodeURIComponent(params.id)}`, { cache: "no-store" }); const payload = await response.json(); if (!response.ok || !payload.ok || !payload.players?.[0]) throw new Error(payload.error || "اطلاعات بازیکن پیدا نشد"); const normalized = { ...normalizeStats(payload.players[0]), extras: payload.extras || {} }; if (active) setData(normalized); } catch (err) { if (active) setError(err?.message || "خطا در دریافت اطلاعات بازیکن"); } finally { if (active) setLoading(false); } } load(); return () => { active = false; }; }, [params.id]);
  const totals = useMemo(() => { const stats = data?.stats || []; return { appearances: sum(stats, "games", "appearances"), minutes: sum(stats, "games", "minutes"), goals: sum(stats, "goals", "total"), assists: sum(stats, "goals", "assists"), shots: sum(stats, "shots", "total"), keyPasses: sum(stats, "passes", "key"), tackles: sum(stats, "tackles", "total"), yellow: sum(stats, "cards", "yellow") }; }, [data]);
  if (loading) return <main className="mobile-shell p-6"><div className="rounded-3xl bg-white/5 p-8 text-center text-sm text-slate-400">در حال دریافت پرونده کامل بازیکن…</div></main>;
  if (error || !data) return <main className="mobile-shell p-6"><Link href="/players" className="mb-5 inline-flex items-center gap-2 text-sm text-sky-400"><ArrowRight size={17}/> بازیکنان</Link><div className="rounded-3xl border border-red-400/20 bg-red-400/5 p-5 text-sm text-red-300">{error || "بازیکن پیدا نشد."}</div></main>;
  const { player, stats, extras } = data; const first = stats[0] || {}; const games = first.games || {}; const transfers = (extras.transfers || []).flatMap((item) => item?.transfers || []); const trophies = extras.trophies || []; const sidelined = extras.sidelined || [];
  const compareId = encodeURIComponent(player.id || params.id);
  return <main className="mobile-shell pb-10">
    <header className="px-5 pb-5 pt-6"><Link href="/players" className="mb-5 inline-flex items-center gap-2 text-xs text-sky-400"><ArrowRight size={16}/> همه بازیکنان</Link><div className="glass rounded-3xl p-5"><div className="flex items-center gap-4"><div className="h-20 w-20 overflow-hidden rounded-3xl bg-white/5">{player.photo && <img src={player.photo} alt={player.name} className="h-full w-full object-cover" />}</div><div className="min-w-0 flex-1"><p className="text-xs text-sky-400">FOT10 • PLAYER PROFILE</p><h1 className="mt-1 text-2xl font-black">{player.name}</h1><p className="mt-1 text-xs text-slate-500">{player.nationality || "—"} • {player.age ? `${player.age} سال` : "سن نامشخص"}</p></div><FavoriteButton type="player" name={player.name || ""} className="shrink-0" /></div><div className="mt-4 grid grid-cols-1 gap-2 sm:grid-cols-2"><div className="rounded-2xl bg-white/5 p-3"><p className="text-[10px] text-slate-500">پست</p><b>{games.position || "—"}</b></div><div className="rounded-2xl bg-white/5 p-3"><p className="text-[10px] text-slate-500">ریتینگ</p><b className="text-emerald-400">{games.rating || "—"}</b></div></div><Link href={`/players/compare?a=${compareId}`} className="mt-3 flex w-full items-center justify-center gap-2 rounded-2xl border border-sky-400/20 bg-sky-400/10 px-4 py-3 text-sm font-black text-sky-300 transition hover:bg-sky-400/15"><Swords size={17}/> مقایسه با…</Link></div></header>
    <section className="space-y-4 px-5"><div className="grid grid-cols-2 gap-3"><div className="glass rounded-3xl p-4"><Goal className="mb-2 text-emerald-400" size={19}/><p className="text-xs text-slate-500">گل</p><b className="text-2xl">{totals.goals}</b></div><div className="glass rounded-3xl p-4"><Trophy className="mb-2 text-sky-400" size={19}/><p className="text-xs text-slate-500">پاس گل</p><b className="text-2xl">{totals.assists}</b></div><div className="glass rounded-3xl p-4"><BarChart3 className="mb-2 text-violet-400" size={19}/><p className="text-xs text-slate-500">بازی / دقیقه</p><b className="text-xl">{totals.appearances} / {totals.minutes}</b></div><div className="glass rounded-3xl p-4"><Star className="mb-2 text-amber-400" size={19}/><p className="text-xs text-slate-500">شروع</p><b className="text-2xl">{sum(stats, "games", "lineups")}</b></div></div><div className="glass rounded-3xl p-4"><h2 className="mb-4 font-black">آمار عملکرد</h2><div className="grid grid-cols-2 gap-3 text-sm"><span>شوت: <b>{totals.shots}</b></span><span>پاس کلیدی: <b>{totals.keyPasses}</b></span><span>تکل: <b>{totals.tackles}</b></span><span>کارت زرد: <b>{totals.yellow}</b></span><span>دریبل موفق: <b>{sum(stats, "dribbles", "success")}</b></span><span>دوئل برده: <b>{sum(stats, "duels", "won")}</b></span></div></div><div className="glass rounded-3xl p-4"><h2 className="mb-4 font-black">آمار کامل رقابت‌ها</h2>{stats.length ? <div className="space-y-2">{stats.map((entry, index) => <div key={`${entry.league?.id || index}-${entry.team?.id || index}`} className="rounded-2xl bg-white/5 p-3"><div className="flex items-center gap-3"><div className="h-9 w-9 overflow-hidden rounded-xl bg-white/5">{entry.league?.logo && <img src={entry.league.logo} alt="" className="h-full w-full object-contain"/>}</div><div className="min-w-0 flex-1"><b className="block truncate">{entry.league?.name || "رقابت نامشخص"}</b><p className="text-[11px] text-slate-500">{entry.team?.name || "تیم نامشخص"}</p></div><span className="text-xs text-slate-400">{entry.games?.rating || "—"} ⭐</span></div><div className="mt-3 grid grid-cols-4 gap-2 text-[11px] text-slate-400"><span>{entry.games?.appearances ?? 0} بازی</span><span>{entry.games?.minutes ?? 0} دقیقه</span><span>{entry.goals?.total ?? 0} گل</span><span>{entry.goals?.assists ?? 0} پاس</span></div></div>)}</div> : <p className="text-sm text-slate-500">آمار رقابتی موجود نیست.</p>}</div>{transfers.length > 0 && <div className="glass rounded-3xl p-4"><h2 className="mb-4 flex items-center gap-2 font-black"><History size={18}/> سابقه انتقالات</h2><div className="space-y-2">{transfers.slice(0, 12).map((item, index) => <div key={`${item.date || index}-${index}`} className="rounded-2xl bg-white/5 p-3"><div className="flex items-center justify-between gap-3"><b className="text-sm">{item.teams?.out?.name || "—"} ← {item.teams?.in?.name || "—"}</b><span className="text-[10px] text-slate-500">{formatDate(item.date)}</span></div><p className="mt-1 text-[11px] text-slate-500">{item.type || "انتقال"}</p></div>)}</div></div>}{trophies.length > 0 && <div className="glass rounded-3xl p-4"><h2 className="mb-4 flex items-center gap-2 font-black"><Trophy size={18}/> افتخارات</h2><div className="grid grid-cols-1 gap-2">{trophies.slice(0, 12).map((item, index) => <div key={`${item.league?.id || item.league || index}-${item.season || index}`} className="rounded-2xl bg-white/5 p-3"><b className="text-sm">{item.league?.name || item.league || "رقابت"}</b><p className="text-[11px] text-slate-500">{item.season || "—"} • {item.place || "—"}</p></div>)}</div></div>}{sidelined.length > 0 && <div className="glass rounded-3xl p-4"><h2 className="mb-4 flex items-center gap-2 font-black"><Shield size={18}/> مصدومیت / محرومیت</h2><div className="space-y-2">{sidelined.slice(0, 8).map((item, index) => <div key={`${item.start || index}-${index}`} className="rounded-2xl bg-white/5 p-3"><b className="text-sm">{item.type || item.reason || "مورد ثبت‌شده"}</b><p className="text-[11px] text-slate-500">{formatDate(item.start)} تا {formatDate(item.end)}</p></div>)}</div></div>}<div className="rounded-3xl border border-sky-400/10 bg-sky-400/5 p-4 text-xs leading-6 text-slate-400">این صفحه آمار بازیکن را مستقل از شماره پیراهن نمایش می‌دهد؛ شماره ۱۰ فقط هویت و امضای FOT10 است.</div></section>
  </main>;
}
